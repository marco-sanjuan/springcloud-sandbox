spring.application.name=stock-service
spring.cloud.config.uri=${ENV_CONFIG_URL:#{http://localhost:8888}}

#Configure active profiles here (or pass it as argument),
# because it is necessary before reading remote config files.
spring.profiles.active=dev

management.endpoints.web.exposure.include=*